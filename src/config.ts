// Questions API
export const TEST_MODE = true; // true to generate dummy questions, false for AI questions
export const MIN_PARAGRAPH_LENGTH = 100; // smallest "paragraph" allowed

// Doc
export const AUTOSAVE_DELAY = 1000; // in ms
export const MIN_PAPER_DIMS = {
    height: 11 * 96 * 0.9,
    width: 8.5 * 96 * 0.9
}; // in px

// Authentication
export const MIN_PASSWORD_LENGTH = 8;